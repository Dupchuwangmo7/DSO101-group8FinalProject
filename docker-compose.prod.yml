version: '3.8'

services:
  # Production-only: Add MongoDB locally if needed
  # mongodb:
  #   image: mongo:6.0
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: admin
  #     MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
  #     MONGO_INITDB_DATABASE: semzung
  #   volumes:
  #     - mongodb_data:/data/db
  #   ports:
  #     - "27017:27017"
  #   networks:
  #     - semzung-network
  #   restart: unless-stopped

volumes:
  # mongodb_data:
  #   driver: local

networks:
  semzung-network:
    driver: bridge
